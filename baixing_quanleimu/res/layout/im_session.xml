<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <ListView
        android:id="@+id/char_history_p"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="bottom"
        android:divider="@android:color/transparent"
        android:dividerHeight="0px"
        android:layout_weight="1.0"
        android:background="#FFFFFF" >

        <!--LinearLayout
            android:id="@+id/im_content_parent"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="bottom"
            android:orientation="vertical"
            android:paddingLeft="10dp"
            android:paddingRight="10dp" >
        </LinearLayout-->
    </ListView>

    <RelativeLayout
        android:id="@+id/im_input_action_bar"
        android:layout_width="fill_parent"
        android:layout_height="41dp"
        android:layout_marginBottom="2dp"
        android:background="@drawable/bg_talk_send" >

        <EditText
            android:id="@+id/im_input_box"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginLeft="4dp"
            android:layout_marginRight="4dp"
            android:layout_toLeftOf="@+id/im_send_btn"
            android:background="@drawable/bg_talk_input"
            android:hint="最多可输入140个字"
            android:maxLength="140" >
        </EditText>

        <ImageButton
            android:id="@+id/im_send_btn"
            android:layout_width="51dp"
            android:layout_height="36dp"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:layout_marginRight="4dp"
            android:background="@android:color/transparent"
            android:scaleType="fitCenter"
            android:src="@drawable/draw_send_btn" />
    </RelativeLayout>

</LinearLayout>